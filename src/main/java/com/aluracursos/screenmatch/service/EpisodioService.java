package com.aluracursos.screenmatch.service;

import com.aluracursos.screenmatch.dto.EpisodioDTO;
import org.springframework.stereotype.Service;

import java.util.List;

@Service
public class EpisodioService {

  public List<EpisodioDTO> getTemporada(Long id, Integer temporada) {
    // Implementación de búsqueda de episodios por temporada y serie
    // Retorna una lista de episodios DTO con los datos necesarios
    return List.of(); // Placeholder para retornar una lista vacía
  }

}
